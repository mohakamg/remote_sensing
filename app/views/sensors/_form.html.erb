<%= form_with(model: sensor, local: true) do |form| %>
  <% if sensor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sensor.errors.count, "error") %> prohibited this sensor from being saved:</h2>

      <ul>
      <% sensor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :Sensor_ID %>
    <%= form.text_field :sens_id, id: :sensor_sens_id %>
  </div>

  <div class="field">
    <%= form.label :organization_id %>
    <%= form.collection_select :organization_id, Organization.all, :id, :name %>
  </div>

  <div class="field">
    <%= form.label :Sensor_Name %>
    <%= form.text_field :sens_name, id: :sensor_sens_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
